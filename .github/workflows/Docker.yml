name: Docker

on:
  workflow_call:
    
jobs:

  docker:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Setup Docker Build
      uses: docker/setup-buildx-action@v3.10.0

    - name: Docker Login
      uses: docker/login-action@v3.4.0
      with:
        username: jonatasrosalvo
        password: ${{ secrets.DOCKERHUB_PASSWD }}
          
